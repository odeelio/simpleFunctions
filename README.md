# simpleFunctions
Collection of small, simple functions to provide logic-based solutions to common problems.

This will just contain folders containing .py files with one small function.

They all run as-is.
